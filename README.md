## Environment Setup

Create and activate the Conda environment:

```bash
conda create -n PhotonicSensor python=3.10 -y
conda activate PhotonicSensor
pip install -r requirements.txt
pip freeze > requirements.txt